// Converts the entire string to lower case

let variableName = "thisIsInCamelCase";
variableName.toLowerCase(); // === "thisisincamelcase"

// Preserves punctuation and spacing

let sentence = "Hello to all the Presidents and Vice-Presidents in the room!";
sentence.toLowerCase() = "hello to all the presidents and vice-presidents in the room!";

"HELLO".toLowerCase(); // return "hello"

// Method with no parameters but must include brackets
