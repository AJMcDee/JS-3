// The only string PROPERTY
// Can also be used for arrays

const myName = "Anna Jean McDougall";
let nameLength = myName.length;

// We can use this to access the last item using bracket notation
// .length always returns us one number higher than the final index
// because indices begin counting from 0

let lastChar = myName[nameLength - 1]; // return the final L from my name
