// Converts the entire string to upper case
// UPPER CASE LOOKS LIKE THIS : I FEEL LIKE I'M YELLING

let variableName = "thisIsInCamelCase";
variableName.toUpperCase(); // === "THISISINCAMELCASE"

// Preserves punctuation and spacing

const sentence = "Hello to all the Presidents and Vice-Presidents in the room!";
sentence.toUpperCase() = "HELLO TO ALL THE PRESIDENTS AND VICE-PRESIDENTS IN THE ROOM!";

"hello".toUpperCase(); // return "HELLO"

// Method with no parameters but must include brackets
